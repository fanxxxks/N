# AlphaGPT A-share research and paper-trading dependencies
# Python 3.10+ recommended (tested on 3.13)
# Human spec (plain names only).  Exact pins live in requirements.txt,
# generated by: python scripts/freeze_lock.py

torch
numpy
pandas

# Portfolio layer (T3-01): constrained portfolio QP on CVXPY/OSQP
cvxpy

# Default formula searcher (T2-03 admission verdict): strongly-typed GP via
# DEAP's official GP module is the production default searcher, so DEAP is
# a base dependency; TPE (Optuna) stays optional (protocol baseline ladder).
deap

akshare
duckdb
pyarrow

streamlit
plotly
loguru
tqdm

# Typed artifact schemas (P7-C, docs/p7_artifact_schema_contract.md):
# strategy/protocol/backtest/paper-state boundary artifacts are validated
# with Pydantic at write/read time; pinned directly instead of relying on
# FastAPI's transitive dependency.
pydantic

fastapi
uvicorn
psutil

python-dotenv
PyYAML
