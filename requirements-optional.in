# Optional research and testing dependencies.
# Human spec (plain names only).  Exact pins live in
# requirements-optional.txt, generated by: python scripts/freeze_lock.py

pytest
# starlette's TestClient (used by tests/test_webapi.py) requires httpx2.
httpx2
# pandas 3.x computes rank-IC via scipy.spearmanr at runtime.
scipy
# Phase-2 search baselines (T2-02): TPE/multi-objective search via Optuna's
# samplers (the protocol's TPE baseline ladder).  DEAP/GP moved to the base
# requirements (P0-01): GP is the production default searcher.
optuna
# Parallel test execution (PR3/B2+C2 of the 2026-08-31 test-runtime plan):
# local full-suite gates run under pytest-xdist after the parity trial in
# docs/test_runtime_measurement_log.md.  CI keeps its own sharding (PR4).
pytest-xdist
# AkShare 的生产 fallback 数据源与 PIT 成员导入工具（akshare_client.py 的
# fallback 链与 scripts/import_pit_universe.py 均懒加载；tests/test_akshare_client.py
# 无 skip 的无条件 import——契约闭包必须包含，否则全量收集即失败）。
baostock
